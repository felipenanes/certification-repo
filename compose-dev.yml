services:
  postgres:
    image: postgres:15-alpine
    container_name: certification-postgres
    environment:
      POSTGRES_DB: certification_db
      POSTGRES_USER: certification_user
      POSTGRES_PASSWORD: certification_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: 